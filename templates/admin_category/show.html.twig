{% extends 'base-admin.html.twig' %}

{% block title %}Catégorie{% endblock %}

{% block body %}
    <h1 class="text-title text-terracotta">Catégorie <a href="{{ path('app_admin_category_edit', {'id': category.id}) }}"><i class="fa-solid fa-pen text-pink"></i></a></h1>

    <table class="table">
        <tbody>
            <tr>
                <th class="text-terracotta">Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th class="text-terracotta">Titre</th>
                <td>{{ category.label }}</td>
            </tr>
        </tbody>
    </table>

    <a class="text-decoration-none btn btn-terracotta" href="{{ path('app_admin_category_index') }}"><i class="fa-solid fa-arrow-left-long text-white"></i> Retour à la liste</a>

    {{ include('admin_category/_delete_form.html.twig') }}
{% endblock %}
