{% extends 'base-admin.html.twig' %}

{% block title %}Modifier utilisateur{% endblock %}

{% block body %}
    <h1 class="text-title text-terracotta">Modifier l'utilisateur</h1>

    {{ include('admin_user/_form.html.twig', {'button_label': 'Sauvegarder'}) }}
    {# Alternative #}
    {# {{ form_start(form) }}
    {{ form_widget(form.email) }}
    {{ form_widget(form.plainPassword, { 'attr': {'class': 'invisible'} }) }}
    {{ form_widget(form.firstName) }}
    {{ form_widget(form.lastName) }}
    {{ form_widget(form) }}
    <button class="btn btn-pink">{{ button_label|default('Sauvegarder') }}</button>
    {{ form_end(form) }} #}

    <a class="text-decoration-none btn btn-terracotta" href="{{ path('app_admin_user_index') }}"><i class="fa-solid fa-arrow-left-long text-white"></i> Retour à la liste</a>

    {{ include('admin_user/_delete_form.html.twig') }}
{% endblock %}
